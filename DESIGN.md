# CyberShield — System Design

เอกสารออกแบบระบบ อธิบายว่าแต่ละส่วนทำอะไร ต่อกันยังไง **แสดงอะไรออกมาและทำไมต้องแสดงสิ่งนั้น**

หลักการที่ใช้ตลอดเอกสารนี้: **ทุกอย่างที่แสดงต้องตอบได้ว่าใครดู ดูแล้วทำอะไรต่อ**
ตัวเลขที่ดูแล้วไม่มีอะไรให้ทำ ไม่ควรอยู่บนหน้าจอ

---

## 1. เป้าหมายและขอบเขต

### เป้าหมาย

ตรวจจับการโจมตีเครือข่ายแบบเรียลไทม์บน LAN ขนาดเล็ก แล้วแจ้งเตือนผู้ดูแลภายในไม่กี่วินาที

### เกณฑ์ความสำเร็จ

| ตัวชี้วัด | เป้า | วัดจาก |
|---|---|---|
| จับการโจมตีได้ | ≥ 98% | test set ที่ตัดลายนิ้วมือ OS ออกแล้ว |
| อัตราเตือนผิด | ≤ 0.1% ของ flow ปกติ | เดียวกัน |
| เวลาตั้งแต่ flow จบถึงขึ้นจอ | ≤ 3 วินาที | วัดจริงบน VM |
| ระบบทำงานต่อเนื่อง | 24 ชั่วโมงไม่ต้องแตะ | รันทดสอบจริง |

**ทำไมกำหนดอัตราเตือนผิดไว้เข้มขนาดนี้** เพราะบน LAN ที่มี flow ปกติวันละแสน
อัตรา 1% แปลว่าเตือนผิดวันละพันครั้ง คนดูแลจะปิดระบบทิ้งภายในสัปดาห์เดียว
เรื่องนี้ไม่ใช่ทฤษฎี — ในการทดลองพบว่าโมเดลที่คะแนนรวมต่างกันแค่ 0.0001
มีอัตราเตือนผิดต่างกันได้ 25 เท่า (12 ครั้ง vs 304 ครั้ง)

### สิ่งที่ระบบนี้ตั้งใจไม่ทำ

การเขียนข้อนี้สำคัญพอ ๆ กับเขียนว่าทำอะไร เพราะกันไม่ให้ขอบเขตบวมและกันการเข้าใจผิด

- **ไม่บล็อกการโจมตี** เป็นระบบตรวจจับ (IDS) ไม่ใช่ระบบป้องกัน (IPS) การบล็อกอัตโนมัติ
  ต้องมีความแม่นยำสูงกว่านี้มาก เพราะบล็อกผิดคือทำให้คนทำงานไม่ได้
- **ไม่แกะเนื้อหาข้างในแพ็กเก็ต** วิเคราะห์ระดับ flow เท่านั้น จึงไม่ละเมิดความเป็นส่วนตัว
  และทำงานได้แม้ traffic เข้ารหัส
- **ไม่รองรับการโจมตีที่ไม่เคยเห็นตอนเทรน** โมเดลรู้จัก 4 ประเภทเท่านั้น
- **ไม่รองรับหลายผู้ใช้** มีผู้ดูแลคนเดียว ไม่มีระบบสิทธิ์
- **ไม่เก็บ traffic ดิบ** เก็บแค่ผลการทำนาย ย้อนกลับไปดู flow ต้นทางไม่ได้

---

## 2. สถาปัตยกรรม

```
เครือข่าย
   │
   ├─ Network Sensor (nfstream) ─────┐
   │     สรุป flow เป็น 78 ตัวเลข     │
   │                                  │  POST /internal/event
   ├─ HTTP Sensor (mitmproxy) ────────┤  header: X-Internal-Token
   │     ดึงข้อความคำขอ HTTP           │
   │                                  ▼
   │                          ┌───────────────┐
   │                          │    FastAPI    │
   │                          │  :8000        │
   │                          ├───────────────┤
   │                          │ Flow Model    │ GRU (10, 71)
   │                          │ Intrusion Mdl │ LSTM  (offline)
   │                          │ Injection Mdl │ ยังไม่เริ่ม
   │                          ├───────────────┤
   │                          │ WindowBuffer  │
   │                          │ SQLite (WAL)  │
   │                          └───────┬───────┘
   │                                  │ WebSocket /ws (ทางเดียว)
   │                                  ▼
   │                          ┌───────────────┐
   └──────────────────────────│ nginx :80     │
                              │ React SPA     │
                              └───────────────┘
```

ทุกอย่างรันบน VM Linux เครื่องเดียวบน LAN บ้าน

### ทำไมรวมทุกอย่างไว้เครื่องเดียว

เป็นการตัดสินใจที่ตั้งใจ ไม่ใช่ความขี้เกียจ

- ขนาดงานไม่ต้องการการกระจาย — flow บน LAN บ้านไม่ถึงพันต่อวินาที
- ลดจุดที่จะพัง ไม่มีเรื่องเครือข่ายระหว่าง service ให้ดีบัก
- **WindowBuffer ต้องมีที่เดียว** ถ้ากระจายหลายเครื่อง แต่ละเครื่องจะเห็นลำดับ flow
  ไม่เหมือนกัน ซึ่งทำให้ผลต่างจากตอนเทรน
- ย้ายไปหลายเครื่องทีหลังได้ เพราะ interface ระหว่างส่วนเป็น HTTP กับ WebSocket อยู่แล้ว

### ทำไม sensor ไม่โหลดโมเดลเอง

sensor รันด้วยสิทธิ root (ต้องใช้เพื่อดักดู traffic) และอาจมีหลายตัว

ถ้าแต่ละตัวโหลดโมเดลเอง จะได้ WindowBuffer คนละอัน ลำดับ flow ที่แต่ละโมเดลเห็น
จะไม่ตรงกับตอนเทรน และผลจะเพี้ยนโดยไม่มีใครรู้

**sensor จึงมีหน้าที่เดียว: แปลง traffic เป็นตัวเลขแล้วส่งต่อ** ตัดสินใจทั้งหมดอยู่ที่ backend

---

## 3. สัญญาระหว่างส่วนประกอบ

### 3.1 Sensor → Backend

```json
POST /internal/event
X-Internal-Token: <shared secret>

{
  "features": { "Dst Port": 80.0, "Flow Duration": 1523.0, ... },
  "source_ip": "192.168.1.42"
}
```

**ต้องส่ง feature ครบ 78 ตัว** ชื่อตรงกับ `feature_cols.json` เป๊ะ

**`source_ip` ใช้แสดงผลเท่านั้น ไม่ได้ป้อนเข้าโมเดล** เพราะชุดข้อมูลที่ใช้เทรนไม่มีคอลัมน์นี้
ถ้าเอาเข้าโมเดลจะเป็น train/serve mismatch ทันที

### 3.2 ลำดับการแปลงข้อมูล — ห้ามสลับ

```
78 ตัวเลขดิบจาก sensor
  ↓ scaler.transform(78)          scaler ถูก fit ด้วย 78 คอลัมน์
  ↓ ตัด fingerprint 7 ตัว → 71     ตัดหลัง scale ไม่ใช่ก่อน
  ↓ WindowBuffer สะสมครบ 10
  ↓ model.predict(1, 10, 71)
Prediction Event
```

**ทำไมต้อง scale ก่อนแล้วค่อยตัด** เพราะ scaler จำ mean/std ของทั้ง 78 คอลัมน์ไว้
ถ้าตัดก่อน ตัวเลขจะเข้าคอลัมน์ผิดทั้งแถว **โดยไม่มี error ให้เห็น** มีแค่ผลลัพธ์ที่ผิดเงียบ ๆ

ความผิดพลาดประเภทนี้เป็นสิ่งที่เกิดขึ้นจริงมาแล้วสองครั้งในโปรเจกต์นี้
(คอลัมน์ลำดับแถวหลุดเข้าเป็น feature และค่าอนันต์ที่เก็บเป็นข้อความ)
ทั้งสองครั้งไม่มี error มีแค่คะแนนที่ดีเกินจริง

### 3.3 Backend → Dashboard

```json
{
  "model": "best_GRU",
  "attack_class": "DDoS",
  "confidence": 0.9723,
  "source_ip": "192.168.1.42",
  "timestamp": "2026-08-03T10:15:32Z",
  "is_alert": true
}
```

**WebSocket ส่งทางเดียว** browser ส่งข้อมูลเข้ามาไม่ได้เลย

ถ้าเปิดให้ browser ส่ง payload เข้าโมเดลได้ ใครก็ตามที่เปิดหน้าเว็บได้จะยิงข้อมูลปลอมเข้าระบบได้
ซึ่งทำให้บันทึกเหตุการณ์เชื่อถือไม่ได้ทั้งหมด

หน้า `/test` ที่ให้ยิง payload เองมีไว้สำหรับ demo และดีบัก **ต้องบันทึกแยกจากเหตุการณ์จริง**
ไม่งั้นข้อมูลสาธิตจะปนกับข้อมูลจริงในฐานข้อมูล

---

## 4. การตัดสินใจสำคัญ และเหตุผล

รูปแบบ: ตัดสินใจอะไร / ทางเลือกที่พิจารณา / เลือกเพราะอะไร / แลกอะไรไป

### D1 — ใช้ GRU ไม่ใช่ LSTM หรือ BiLSTM

**ทางเลือก** SimpleRNN, GRU, LSTM, BiLSTM

**หลักฐาน** ทดสอบ 3 ค่าสุ่ม × 4 โมเดล พบว่าผู้ชนะเปลี่ยนไปทุกรอบ
ความต่างระหว่างโมเดล (0.0010) เล็กกว่าความแปรผันจากค่าสุ่มของตัวมันเอง (0.0014)

| model | parameter | คะแนน | ความแปรผัน |
|---|---|---|---|
| SimpleRNN | 40,164 | 0.9719 | ±0.0024 |
| **GRU** | **116,644** | **0.9718** | **±0.0002** |
| BiLSTM | 373,412 | 0.9710 | ±0.0020 |
| LSTM | 154,020 | 0.9709 | ±0.0011 |

**เลือก GRU เพราะ** ให้ผลเทียบเท่าทุกตัวด้วย parameter หนึ่งในสามของ BiLSTM
เทรนเร็วที่สุด และ**ให้ผลนิ่งที่สุดข้ามค่าสุ่ม** ซึ่งสำคัญเมื่อต้องเทรนซ้ำในอนาคต

**แลกอะไรไป** BiLSTM มีอัตราเตือนผิดต่ำกว่าเล็กน้อยในการรันหนึ่งครั้ง (12 vs 25)
แต่ตัวเลขนั้นยังไม่ได้ทดสอบข้ามค่าสุ่ม จึงยังเคลมไม่ได้

**ข้อสังเกตที่ควรบันทึก** การเลือกโมเดลในงานนี้ไม่ควรอ้างคะแนน เพราะทุกตัวเสมอกัน
ควรอ้างต้นทุนและความเสถียรแทน

### D2 — ตัด feature 7 ตัวที่เป็นลายนิ้วมือของ OS ออก

**ทางเลือก** ใช้ครบ 78 ตัว หรือตัดเหลือ 71

**หลักฐาน** feature 4 ตัวที่เกี่ยวกับ TCP header ให้คะแนน **0.9999 ซึ่งสูงกว่าใช้ครบทั้ง 78 ตัว**

feature เหล่านั้น (TCP window size, header length, MSS, พอร์ต) ถูกกำหนดโดยระบบปฏิบัติการ
ไม่ใช่โดยพฤติกรรมการโจมตี และในชุดข้อมูลนี้การโจมตีทั้งหมดมาจากเครื่องผู้โจมตีชุดเดียว

**แปลว่าโมเดลกำลังตอบว่า "flow นี้มาจากเครื่องไหน" ไม่ใช่ "เป็นการโจมตีหรือไม่"**

**เลือกตัดออก แลกกับคะแนนที่ลดจาก 0.9959 เหลือ 0.9534**

**เหตุผล** ตัวเลข 0.9959 ใช้ไม่ได้จริง ผู้โจมตีที่ใช้ระบบปฏิบัติการอื่นจะหลุดทันที
และเครื่อง Linux ที่ใช้งานปกติจะถูกเตือนผิด ส่วน 0.9534 คือความสามารถจริง

### D3 — ทิ้ง window ที่ยังไม่เต็ม ไม่ใช้ Masking layer

**ปัญหา** `windowing.py` วาง padding ไว้ทางซ้าย (ข้อมูลจริงชิดขวา แถวปัจจุบันอยู่ท้ายเสมอ)
แต่ cuDNN รองรับ mask แบบ padding อยู่ขวาเท่านั้น

**ทางเลือก** ปิด cuDNN (ช้าลงหลายเท่า) / เปลี่ยนไป right-padding (ต้องแก้ทั้งระบบ) / ทิ้ง window ที่ไม่เต็ม

**เลือกทิ้ง เพราะมีแค่ 9 อันจาก 468,232 (0.00%)**

**ผลต่อ serving ที่ต้องรู้** โมเดลไม่เคยเห็น window ที่ไม่เต็ม
backend จึงต้องรอสะสมครบ 10 flow ก่อนเริ่มทำนาย ช่วง 9 flow แรกหลัง sensor เริ่มทำงานให้ข้ามไป

### D4 — โค้ดสร้าง window และเลือก feature อยู่ที่เดียว

**ปัญหาเดิม** window ตอนเทรนถูกจัดกลุ่มตาม label ซึ่งตอน serve ทำตามไม่ได้เพราะยังไม่รู้คำตอบ

**วิธีแก้เชิงโครงสร้าง** `common/windowing.py` และ `common/features.py` ถูกเรียกจากทั้งสองฝั่ง
พร้อมฟังก์ชัน `assert_parity()` ที่พิสูจน์ว่าให้ผลตรงกันทุกแถว และรันเป็นเทสต์

**ทำไมไม่แค่เขียนเตือนไว้ในเอกสาร** เพราะเอกสารไม่หยุดโค้ดที่ผิด
กลไกนี้ทำให้ความผิดพลาดแบบเดิมเกิดขึ้นไม่ได้เชิงโครงสร้าง ไม่ใช่แค่หวังว่าจะจำได้

### D5 — SQLite แทน PostgreSQL

**เลือก SQLite** เพราะไม่ต้องติดตั้ง service เพิ่ม ลดจุดที่จะพังบน VM เครื่องเดียว

**เปิด WAL mode** เพื่อให้ dashboard อ่านได้โดยไม่บล็อกการเขียนของ sensor

**schema ตั้งใจไม่ใช้ type เฉพาะของ SQLite** จึงย้ายไป PostgreSQL ได้โดยไม่ต้องแก้ schema
เมื่อไหร่ที่ปริมาณโตเกินหรือต้องการหลายเครื่องเขียนพร้อมกัน

### D6 — ตรวจ schema ตอน startup แล้วยอมให้ล้ม

`FlowModel.__init__` ตรวจสามชั้น: scaler รับกี่ feature, การตัด 78→71 ตรงกับที่โมเดลเทรนไหม,
input shape ของโมเดลตรงกับ pipeline ไหม ถ้าข้อไหนไม่ตรง **FastAPI จะไม่ยอมเริ่มทำงาน**

**ทำไมยอมให้ล้ม** เพราะทางเลือกอีกทางคือ boot ผ่านแล้วทำนายผิดทุกครั้งโดยไม่มีใครรู้
ระบบที่ไม่ทำงานคนจะรู้ทันที ระบบที่ทำงานผิดคนจะไว้ใจมันไปเรื่อย ๆ

---

## 5. Dashboard — แสดงอะไร และทำไม

**หลักการ: ทุกช่องต้องตอบสามคำถาม**

1. ใครดูช่องนี้
2. ตัวเลขนี้คำนวณมาจากอะไร
3. ดูแล้วทำอะไรต่อ

**ถ้าข้อ 3 ตอบไม่ได้ ให้ตัดช่องนั้นทิ้ง** — พื้นที่หน้าจอมีจำกัด ทุกช่องที่ไม่มีประโยชน์
คือช่องที่แย่งความสนใจจากช่องที่มีประโยชน์

### 5.1 แถวบน — สถานะระบบ

| แสดงอะไร | ทำไม | ทำอะไรต่อ |
|---|---|---|
| **สถานะ sensor** เชื่อมต่อ / ไม่เชื่อมต่อ | ถ้า sensor ตาย ระบบเงียบสนิทเหมือนไม่มีการโจมตี ซึ่งอันตรายมาก | ไปเช็ค VM |
| **สถานะ warmup** เหลืออีกกี่ flow | ช่วง 9 flow แรกระบบยังไม่ทำนาย ต้องบอกให้รู้ ไม่งั้นคนจะคิดว่าเสีย | รอ |
| **flow ต่อวินาที** | ค่าฐานปกติของเครือข่าย ถ้าพุ่งผิดปกติคือสัญญาณ | เทียบกับกราฟย้อนหลัง |
| **แจ้งเตือนใน 1 ชั่วโมง** | ตัวเลขเดียวที่บอกว่าตอนนี้ต้องสนใจไหม | ถ้า > 0 เลื่อนไปดูรายการ |

**ที่ตัดออกและเหตุผล** ช่องที่มีแค่สองสถานะเปิด/ปิด (เช่น "ระบบไซเรน") ไม่ควรกินพื้นที่เท่าการ์ดใหญ่
รวมเข้าไปเป็นไอคอนเล็กในแถบสถานะแทน และ "กำลังเชื่อมต่อ" กับ "เชื่อมต่อแล้ว"
คือตัวแปรเดียวกัน ไม่ต้องแยกสองช่อง

### 5.2 กราฟ traffic แบบเรียลไทม์

**แสดง** แพ็กเก็ตต่อวินาที เป็นกราฟเส้นย้อนหลัง 5 นาที

**ทำไมเป็นกราฟเส้นไม่ใช่ตัวเลขเดียว** เพราะสิ่งที่มีความหมายคือ**การเปลี่ยนแปลง** ไม่ใช่ค่า ณ ขณะนั้น
เลข 5,000 ไม่บอกอะไรถ้าไม่รู้ว่าปกติเท่าไหร่ แต่เส้นที่พุ่งจาก 500 เป็น 5,000 ใน 3 วินาทีบอกทันทีว่ามีอะไรผิดปกติ

**ต้องมีสเกลบนแกน** กราฟที่ไม่มีตัวเลขกำกับแกนอ่านไม่ได้ ดูได้แค่ว่า "ขึ้น" หรือ "ลง"
แต่บอกไม่ได้ว่าขึ้นแค่ไหน ซึ่งเป็นข้อมูลที่จำเป็นในการตัดสินใจ

**ควรเพิ่ม: แถบเปรียบเทียบค่าจริงกับค่าที่โมเดลคาด** ในกราฟเดียวกัน
ทำให้เห็นว่าความผิดปกติที่เกิดขึ้นคือสิ่งที่ระบบรู้ตัวหรือไม่รู้ตัว

### 5.3 รายการแจ้งเตือน

**แสดงต่อรายการ** เวลา / ประเภท / ไอพีต้นทาง / ความมั่นใจ

**เรียงตามเวลาล่าสุดก่อน** เพราะการโจมตีที่กำลังเกิดสำคัญกว่าที่จบไปแล้ว

**แสดงความมั่นใจเป็นตัวเลข ไม่ใช่แค่สี** เพราะ 0.81 กับ 0.99 ต่างกันมากในการตัดสินใจว่าจะรีบแค่ไหน
แต่ถ้าแสดงเป็นสีแดงเหมือนกันจะแยกไม่ออก

**สิ่งที่ต้องเขียนกำกับไว้ให้ชัด** ระบบแยก DoS กับ DDoS ได้ไม่สมบูรณ์
ในการทดสอบ DDoS 15.6% ถูกทายเป็น DoS **แต่ไม่เคยมี DoS ถูกทายเป็น DDoS เลยสักครั้ง**

แปลว่าถ้าจอขึ้นว่า "DDoS" ให้เชื่อได้เลย แต่ถ้าขึ้นว่า "DoS" อาจเป็น DDoS ก็ได้
ข้อมูลนี้เปลี่ยนวิธีตอบสนองของคนดูแล จึงต้องบอก ไม่ใช่ซ่อน

### 5.4 ปุ่ม (i) ข้างทุกตัวเลข

**ทุกการ์ดและทุกกราฟต้องมีไอคอน information ที่กดแล้วบอกว่าตัวเลขนี้คำนวณมาจากอะไร**

ตัวอย่างเนื้อหาที่ควรอยู่ในนั้น

> **แพ็กเก็ตต่อวินาที**
> นับจากจำนวนแพ็กเก็ตในทุก flow ที่จบภายในวินาทีนั้น หารด้วยระยะเวลา
> ที่มา: Network Sensor (nfstream) · อัปเดตทุก 1 วินาที

**ทำไมต้องมี** เพราะเมื่อไม่มี คนที่ทำระบบต้องคอยตามอธิบายทุกครั้งที่มีคนถาม
และเมื่อคนทำไม่อยู่ ก็ไม่มีใครตอบได้ว่าตัวเลขนั้นแปลว่าอะไร

ข้อนี้มีผลย้อนกลับมาที่คนออกแบบด้วย — **ถ้าเขียนคำอธิบายไม่ได้ แปลว่ายังไม่เข้าใจว่าทำไมถึงใส่ช่องนั้นไว้**
ซึ่งเป็นสัญญาณว่าควรตัดออก

### 5.5 หน้าทดสอบด้วยตนเอง

**มีไว้ทำไม** สาธิตตอนไม่มีการโจมตีจริง และดีบักโมเดล

**ต้องแยกให้ชัดจากข้อมูลจริง** ติดป้ายว่าเป็นข้อมูลทดสอบ และเก็บคนละที่กับเหตุการณ์จริง
ไม่งั้นสถิติทั้งหมดจะปนเปื้อน

### 5.6 สิ่งที่ dashboard ต้องไม่ทำ

- **ไม่ส่งข้อมูลเข้าโมเดล** — เป็น read-only ทั้งหมด
- **ไม่แสดงเนื้อหาแพ็กเก็ต** — ระบบไม่เก็บอยู่แล้ว และจะเป็นปัญหาความเป็นส่วนตัว
- **ไม่แสดงคะแนนความแม่นยำเป็นตัวเลขเดียว** เช่น "ความแม่นยำ 97%" เพราะตัวเลขนั้นมาจาก
  ชุดข้อมูลทดสอบ ไม่ใช่จากเครือข่ายนี้ การแสดงบนหน้าจอสดทำให้คนเข้าใจผิดว่าเป็นผลปัจจุบัน

---

## 6. โหมดความล้มเหลว

ออกแบบระบบต้องคิดว่าอะไรจะพัง และพังแล้วเกิดอะไร

| สิ่งที่พัง | อาการ | ระบบทำอะไร | คนต้องทำอะไร |
|---|---|---|---|
| sensor ตาย | ไม่มี flow เข้ามา | dashboard ขึ้นสถานะขาดการเชื่อมต่อภายใน 10 วินาที | รีสตาร์ท sensor |
| โมเดลโหลดไม่ได้ | ไฟล์หายหรือ schema ไม่ตรง | **FastAPI ไม่ยอม start** พร้อมบอกว่าไม่ตรงตรงไหน | เช็ค artifacts |
| sensor ส่ง feature ไม่ครบ | คำขอมี feature ขาด | คืน error 422 พร้อมบอกว่าขาดตัวไหน ไม่ทำนาย | แก้ sensor |
| WebSocket หลุด | dashboard ค้าง | client เชื่อมใหม่อัตโนมัติ ระหว่างนั้นดึงประวัติจาก REST | ไม่ต้องทำอะไร |
| ดิสก์เต็ม | เขียนฐานข้อมูลไม่ได้ | ลบข้อมูลเก่ากว่า 30 วันอัตโนมัติ | ตรวจถ้ายังเต็ม |
| flow เข้ามาเร็วเกิน | คิวยาว | ทิ้ง flow ที่เก่าที่สุด แล้วขึ้นเตือนบน dashboard | เพิ่มทรัพยากร |

**หลักการ: ล้มแบบดังกว่าล้มแบบเงียบ** ทุกกรณีข้างบนคนดูแลต้องรู้ว่าเกิดอะไรขึ้น
ไม่มีกรณีไหนที่ระบบทำงานต่อโดยให้ผลที่เชื่อไม่ได้

---

## 7. ความปลอดภัย

| ประเด็น | วิธีจัดการ | ทำไม |
|---|---|---|
| `/internal/event` เปิดให้ใครก็ยิงได้ | shared secret ใน header + ผูกกับ localhost | sensor รันคนละ process ด้วยสิทธิ root ต้องมีวิธียืนยันตัวตน |
| ความลับอยู่ในโค้ด | เก็บใน `.env` และ `.gitignore` ไม่ให้ขึ้น git | ความลับที่ขึ้น git แล้วถือว่ารั่วถาวร |
| dashboard เข้าถึงได้จากภายนอก | nginx ผูกกับ LAN เท่านั้น | ระบบนี้ไม่มีการยืนยันตัวตนผู้ใช้ |
| sensor ต้องใช้สิทธิ root | แยกเป็นคนละ process จาก backend | backend ที่ถูกเจาะจะไม่ได้สิทธิ root ตามไปด้วย |
| HTTP Sensor เห็นข้อมูลที่เข้ารหัส | เก็บเฉพาะ query string และ body สำหรับตรวจ SQLi ไม่บันทึกลงดิสก์ | ลดความเสี่ยงต่อความเป็นส่วนตัว |

---

## 8. สิ่งที่ต้องเฝ้าดูหลัง deploy

โมเดลที่แม่นวันนี้ไม่ได้แปลว่าจะแม่นตลอดไป เครือข่ายเปลี่ยน อุปกรณ์เปลี่ยน การโจมตีเปลี่ยน

| ตัวชี้วัด | สัญญาณอันตราย | ทำอะไร |
|---|---|---|
| สัดส่วนที่ทายว่าเป็นการโจมตี | สูงขึ้นเรื่อย ๆ ทั้งที่ไม่มีเหตุ | โมเดลเริ่มเพี้ยน ตรวจว่ามีอุปกรณ์ใหม่บนเครือข่ายไหม |
| การกระจายค่าความมั่นใจ | กระจุกอยู่แถวเกณฑ์ตัดสิน | เกณฑ์ตั้งไว้ไม่เหมาะกับเครือข่ายนี้ ต้องปรับ |
| เวลาตอบสนอง | เกิน 3 วินาที | มี flow มากเกินกำลัง |
| จำนวน flow ต่อวินาที | ต่างจากค่าปกติมาก | อาจเป็นการโจมตี หรือ sensor มีปัญหา |

**เกณฑ์แจ้งเตือน (`THRESHOLD_FLOW=0.80`) เป็นค่าตั้งต้นที่ยังไม่ได้ปรับเทียบ**

โมเดลเทรนด้วยการถ่วงน้ำหนักกลุ่มที่ข้อมูลน้อย ทำให้ค่าความน่าจะเป็นที่ออกมา
ไม่ตรงกับความน่าจะเป็นจริง ต้องวัด calibration (เช่น Brier score) บนเครือข่ายจริงแล้วปรับใหม่
ก่อนจะเชื่อตัวเลข 0.80 นี้

---

## 9. คำถามที่ยังไม่มีคำตอบ

เขียนไว้ตรง ๆ ว่าอะไรที่ยังไม่รู้ ดีกว่าปล่อยให้คนอ่านคิดว่าคิดครบแล้ว

1. **nfstream ผลิต feature ครบ 78 ตัวชื่อตรงกันได้จริงไหม** ยังไม่ได้ทดสอบ
   nfstream ตั้งชื่อ field ไม่เหมือน CICFlowMeter ที่ใช้สร้างชุดข้อมูล ต้องเขียนตัวแปลงชื่อ
   **นี่คือความเสี่ยงที่ใหญ่ที่สุดที่เหลืออยู่ของโปรเจกต์**

2. **Intrusion Model ป้อนด้วย Network Sensor ไม่ได้** feature แบบ NSL-KDD เช่น
   จำนวนครั้งที่ login ล้มเหลว มาจาก log ของเครื่องปลายทาง ไม่ใช่จาก flow
   ต้องเพิ่ม Host Sensor หรือประกาศให้ชัดว่าโมเดลนี้ใช้แบบ offline เท่านั้น

3. **ยังไม่มีกติกาตัดสินเมื่อสองโมเดลยิงพร้อมกัน** flow เดียวกันเข้าได้ทั้งสองโมเดล
   ต้องกำหนดว่าเชื่อใครก่อน

4. **ค่าฐานปกติของเครือข่ายจริงเป็นเท่าไหร่** ยังไม่ได้วัด ต้องรันเก็บข้อมูลอย่างน้อยหนึ่งสัปดาห์
   ก่อนจะตั้งเกณฑ์แจ้งเตือนได้อย่างมีเหตุผล

5. **โมเดลจะทำงานยังไงบนเครือข่ายที่ไม่ใช่แบบที่เทรนมา** ยังไม่ได้ทดสอบข้ามชุดข้อมูล
   คาดว่าผลจะต่ำกว่า 0.9534 แต่ไม่รู้ว่าเท่าไหร่

---

## 10. ลำดับการพัฒนาต่อ

| ลำดับ | งาน | ทำไมอยู่ตำแหน่งนี้ |
|---|---|---|
| 1 | ตัวแปลงชื่อ feature จาก nfstream | ถ้าข้อนี้ไม่ผ่าน ที่เหลือไม่มีความหมาย |
| 2 | ทดสอบ end-to-end บน VM | พิสูจน์ว่าต่อกันได้จริงก่อนไปทำอย่างอื่น |
| 3 | เก็บค่าฐานปกติหนึ่งสัปดาห์ | ต้องมีก่อนจึงตั้งเกณฑ์ได้ |
| 4 | ปรับเทียบเกณฑ์แจ้งเตือน | อาศัยข้อ 3 |
| 5 | ปุ่ม (i) บน dashboard ครบทุกช่อง | ทำได้ขนานกับข้ออื่น |
| 6 | Injection Model | เป็นความสามารถใหม่ ไม่ได้บล็อกอะไร |
| 7 | ทดสอบข้ามชุดข้อมูล | งานวิจัย ไม่ได้บล็อกการใช้งาน |
